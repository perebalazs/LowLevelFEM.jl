# Nodal vs Element Fields

Many APIs operate on nodal values, while others assemble or evaluate elementwise quantities.

## Practical Guidance

- Use `isNodal` and `isElementwise` to inspect representation.
- Convert with `elementsToNodes` and `nodesToElements` where needed.

## Status

Placeholder page.
